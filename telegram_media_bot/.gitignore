# Secrets — never commit the real .env, only .env.example
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Runtime data (sqlite DB, temp media files)
data/
!data/.gitkeep

# Test/tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
